# 测试环境 — 模拟内测用户
# 用法: cp .env.test .env && uvicorn main:app --reload

DEEPSEEK_API_KEY=sk-your-key-here
DEEPSEEK_BASE_URL=https://api.deepseek.com/v1
DEEPSEEK_MODEL=deepseek-v4-flash

# 测试：开邀请码、独立空数据库
REQUIRE_INVITE=true
DATABASE_PATH=data/test.db
